#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen_recursion - check the code
 * @s: a pointer parameter
 * Return: an integer
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
