#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strchr - check the code
 * @s: a pointer to the memory area of s
 * @c: the character to look for in s
 * Return: a pointer to.
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
